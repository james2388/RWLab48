<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:id="@+id/btnGallery"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gallery App" />
    <Button
        android:id="@+id/btnWebBrowser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Web Browser"
        android:layout_below="@+id/btnGallery"
        android:layout_marginRight="20dp"/>
    <Button
        android:id="@+id/btnMap"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Open map"
        android:layout_below="@+id/btnWebBrowser" />
    <Button
        android:id="@+id/btnGalleryImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choose image"
        android:layout_below="@+id/btnMap" />
    <Button
        android:id="@+id/btnCameraPhoto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Make a photo"
        android:layout_toRightOf="@+id/btnWebBrowser" />
    <Button
        android:id="@+id/btnCameraVideo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Record a video"
        android:layout_below="@+id/btnCameraPhoto"
        android:layout_toRightOf="@+id/btnWebBrowser" />
    <Button
        android:id="@+id/btnDictophone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voice recording"
        android:layout_below="@+id/btnCameraVideo"
        android:layout_toRightOf="@+id/btnWebBrowser"/>

</RelativeLayout>
